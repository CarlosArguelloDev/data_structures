# Tuplas
"""  
  Se suelen usar no para modificar valores si no mas bien para dar información de un conjunto
  de elementos que no se vayan a modificar
"""
# Se pueden acceder de manera similar a las listas
par = (1, 2, 3, 4)
lista = [1, 2, 3, 4]

print(par)
print(lista)

lista[0] = 5
par[0] = 5

print(par[0])
print(lista[0])
